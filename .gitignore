# .NET
bin/
obj/
*.user
*.suo
.vs/

# Exported game builds (godot --export → repo-root build/)
build/

# Rider / JetBrains
.idea/
*.DotSettings.user

# Godot 4 (.NET)
.godot/
.import/
export.cfg
# export_presets.cfg IS tracked: its paths are repo-relative, so the CLI
# export setup reproduces from a clean clone. (Godot ignores it by default.)
*.translation

# Claude Code (personal/local settings)
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db
